# ADR 0002: deterministic local-first reference stack

Status: accepted

The primary reviewer path runs without cloud accounts or proprietary services. Local adapters preserve the semantics of feature storage, registry aliases and telemetry while keeping setup bounded. Optional production substitutions must retain the shared contracts and cannot become requirements for the core demo.
